<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/iconos/Logo_Dindog.svg" rel="shortcut icon">
    <title>Detalle de la Reserva</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheet/reserva/detalles.css">
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="container mt-5 mb-5">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h2 class="mb-0">Detalle de la Reserva</h2>
            </div>
            <div class="card-body">
                <h3 class="card-title">Información General</h3>
                <p><strong>Fecha de Inicio:</strong> <%= reserva.fechaInicio.toLocaleDateString() %></p>
                <p><strong>Fecha de Fin:</strong> <%= reserva.fechaFin.toLocaleDateString() %></p>
                <p><strong>Hora de Inicio:</strong> <%= reserva.horaInicio %></p>
                <p><strong>Hora de Fin:</strong> <%= reserva.horaFin %></p>
                <p><strong>Cuidador:</strong> <%= reserva.cuidador.nombre %></p>
                <p><strong>Estado:</strong> <%= reserva.estado %></p>
                <p><strong>Precio Total:</strong> <%= reserva.precioTotal %>€</p>
                <% if(reserva.estado === 'pendiente' || reserva.estado === 'aceptada') { %>
                    <form action="/reservas/cancelar/<%= reserva._id %>?_method=PATCH" method="post" onsubmit="return confirm('¿Estás seguro que deseas cancelar esta reserva?');">
                        <button type="submit" class="btn btn-danger mt-3">Cancelar</button>
                    </form>                    
                <% } %>
            </div>
        </div>
    </div>
    <%- include('../partials/footer') %> 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
