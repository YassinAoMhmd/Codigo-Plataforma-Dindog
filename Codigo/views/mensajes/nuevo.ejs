<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/iconos/Logo_Dindog.svg" rel="shortcut icon">
    <title>Enviar Mensaje</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheet/mensajes/nuevo.css">
</head>
<body>
    <%- include('../partials/navbar.ejs') %>
    <main class="container mt-5">
        <% if (cuidador) { %>
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h1 class="h3 mb-0">Enviar Mensaje a <%= cuidador.nombre %></h1>
                </div>
                <div class="card-body">
                    <form action="/mensajes/enviar/<%= cuidador._id %>" method="post">
                        <div class="mb-3">
                            <label for="contenido" class="form-label">Mensaje:</label>
                            <textarea name="contenido" id="contenido" class="form-control" rows="4" required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">Enviar</button>
                    </form>
                </div>
            </div>
        <% } else { %>
            <div class="alert alert-danger">
                No se pudo encontrar la información del cuidador. Por favor, inténtalo de nuevo.
            </div>
        <% } %>
    </main>
    <%- include('../partials/footer.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
